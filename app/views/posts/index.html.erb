<h3>Posts List</h3>

<table class="table">
  <tbody>
  <% @posts.each do |post| %>
    <tr>
      <td>
        <%= link_to post.title, post.url, target: "_blank" %>
      </td>
    </tr>
  <% end %>
  </tbody>
</table>

<%== pagy_bootstrap_nav(@pagy) %>
